# AGENTS.md – Rust API Skeleton Example

- Toolchain: Rust 1.79.0 (stable), cargo; nightly not required.
- Start dev server: `cargo run`.
- Tests: `cargo test` for unit; `just it` for integration (requires Docker).
- Do not modify files in `/nix`; they are generated.
